'use client';

import React, { ReactElement } from 'react';

import Error from '@/shadcn/blocks/error-page/error-page';

function NotFound(): ReactElement {
  return <Error />;
}

export default NotFound;
